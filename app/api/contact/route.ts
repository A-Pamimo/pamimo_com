// This file is disabled because API routes are not compatible with "output: export".
// To use API routes on Cloudflare Pages, you must use the @cloudflare/next-on-pages adapter
// or deploy as a standard Next.js app (not static export).
// For now, the contact form simulation in GameCommsRelay.tsx handles the interaction.
export {};